export interface WorkExperienceEntry {
  role: string;
  company: string;
  start: string;
  end: string;
  techstack: string;
}

export interface EducationEntry {
  title: string;
  organization: string;
  date: string;
}
