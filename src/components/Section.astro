---
import SectionHeader from "./SectionHeader.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class="mb-4">
  <SectionHeader title={title} />
  <slot />
</div>
